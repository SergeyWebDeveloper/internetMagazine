import {FETCH_PHONE_ID_SUCCESS} from 'constants';

const initialState={};

export default (state=initialState,{type,payload}) => {
	switch (type){
		case FETCH_PHONE_ID_SUCCESS:
			return Object.assign({},state,payload);
		default:
			return state;
	}
}